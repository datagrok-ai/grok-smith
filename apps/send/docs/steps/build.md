# Steps for the SEND application build

1. Implement the TypeScript-based XPT parser. Use file in `../../data` for debugging.
2. Create database 
  Each domain should be represented by one table, with the proper
  foreign key references (like to USUBJID).
3. Build views/main.md
4. Implement views/study-view.md
