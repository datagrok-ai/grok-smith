# Required
GITHUB_TOKEN=ghp_...
REPO=your-org/grok-smith

# Auth: pick ONE
# Option 1: Claude Max/Pro subscription (recommended)
#   Your ~/.claude/ is mounted automatically — just run `claude login` in the container once
# Option 2: API key
# ANTHROPIC_API_KEY=sk-ant-...

# Session config
BRANCH=feature/my-task
# TASK=Implement CSV export for the study list page

# Git identity
GIT_USER_NAME=AI Developer
GIT_USER_EMAIL=ai-dev@datagrok.ai

# Optional: Slack notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Optional: PR settings
REVIEWERS=teammate1,teammate2
PR_LABELS=ai-generated

# Optional: ttyd port (for running multiple sessions)
TTYD_PORT=7681
